# ITC Global React - Frontend

## 📋 Обзор

Frontend приложение для инвестиционной платформы ITC Global, построенное на React с использованием современных технологий и паттернов разработки.

## 🛠 Технологический стек

- **React 18** - Основной фреймворк
- **React Router DOM** - Роутинг
- **Zustand** - Управление состоянием
- **Axios** - HTTP клиент
- **Socket.IO Client** - WebSocket соединения
- **Vite** - Сборщик и dev-сервер
- **CSS Modules** - Стилизация

## 🏗 Архитектура приложения

### Структура папок
```
src/
├── components/           # React компоненты
│   ├── entryes/         # Компоненты входа/регистрации
│   ├── landing/         # Лендинг страница
│   ├── main/            # Главный компонент приложения
│   ├── notifications/   # Система уведомлений
│   └── USER/            # Пользовательские компоненты
├── JS/                  # JavaScript модули
│   ├── auth/           # Аутентификация
│   ├── controllers/    # Контроллеры
│   ├── pages/          # Страницы
│   ├── services/       # Сервисы
│   ├── utils/          # Утилиты
│   └── websocket/      # WebSocket клиент
├── FONTS/              # Шрифты
├── IMG/                # Изображения
├── App.jsx             # Главный компонент
├── main.jsx            # Точка входа
└── index.css           # Глобальные стили
```

## 🔐 Система аутентификации

### Основные компоненты
- **`entryes.jsx`** - Контейнер для форм входа/регистрации
- **`login.jsx`** - Форма входа с валидацией
- **`registration.jsx`** - Форма регистрации с валидацией
- **`ref-capture.jsx`** - Обработка реферальных ссылок

### Функциональность
- ✅ **Вход/регистрация** с полной валидацией полей
- ✅ **Реферальные ссылки** (`/ref/:code`) с автозаполнением
- ✅ **JWT токены** (access + refresh) с автоматическим обновлением и ротацией токенов
- ✅ **CSRF защита** с токенами
- ✅ **Валидация форм** на фронтенде
- ✅ **Капча** для защиты от ботов
- ✅ **Проверка чекбоксов** (тест на человечность, согласие на обработку данных)

### Валидация форм
```javascript
// Валидация email
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

// Валидация пароля (минимум 6 символов)
if (formData.password.length < 6) {
  emitEntryError('Пароль должен содержать минимум 6 символов');
  return;
}

// Проверка чекбоксов
const humanTestCheckbox = document.getElementById('user-login-humanyly-test');
if (!humanTestCheckbox.checked) {
  emitEntryError('Необходимо пройти тест на человечность');
  return;
}
```

## 🏠 Главное приложение (Main)

### Навигация
- **Динамическое меню** с активными состояниями
- **Автоматическое обновление заголовков** в зависимости от раздела
- **Роутинг** между различными разделами

### Разделы приложения
1. **Профиль пользователя** (`/personal-room`)
2. **Партнерская программа** (`/personal-room/partners`)
3. **Счета** (`/personal-room/accounts`)
4. **Транзакции** (`/personal-room/transactions`)
5. **Отчеты** (`/personal-room/reports`)
6. **Документы KYC** (`/personal-room/documents`)
7. **Управление аккаунтом** (настройки профиля)

### Аватары
- ✅ **Автоматическая загрузка** дефолтного аватара при первом входе
- ✅ **Обновление аватаров** при изменении пользователя
- ✅ **Поддержка кастомных аватаров** с правильными URL

## 📊 Компоненты пользователя

### 1. Профиль пользователя (`profile-user.jsx`)
- **Личная информация** с возможностью редактирования
- **История входов** с фильтрацией только логинов
- **Статистика аккаунта**
- **Кнопка настроек** для перехода к управлению аккаунтом

### 2. Счета (`accounts-room.jsx`)
- **Список счетов** с отображением балансов
- **Создание новых счетов** по продуктам
- **Переводы между счетами**
- **Пополнение счетов** с загрузкой чеков
- **Вывод средств** с реквизитами

### 3. Транзакции (`transaction-room.jsx`)
- **История транзакций** с фильтрацией
- **Фильтры по счетам** и периодам
- **Кастомный календарь** для выбора дат
- **Аккордеон-отображение** деталей транзакций

### 4. Отчеты (`report-room.jsx`)
- **Фильтрация отчетов** по счетам, годам, месяцам
- **Опция "за все время"** для периодов
- **Синхронизация фильтров** (год → месяц)
- **Модальное окно** с детальной информацией
- **Аккордеон-таблица** отчетов

### 5. Документы KYC (`docs-room.jsx`)
- **Загрузка паспортов** с шифрованием
- **Загрузка других документов** (инвестиционные правила, банковская информация)
- **Визуальная обратная связь** (загрузка, успех, ошибка)
- **Проверка статуса** уже загруженных документов

## 🔔 Система уведомлений

### Типы уведомлений
- **SUCCESS** - Успешные операции
- **ERROR** - Ошибки
- **INFO** - Информационные сообщения
- **ATTENTION** - Предупреждения

### Компоненты
- **`ContainerNotification`** - Контейнер для отображения уведомлений
- **`InfoModal`** - Модальное окно для INFO уведомлений
- **`notification-info.jsx`** - Компонент INFO уведомления

### Функциональность
- ✅ **Real-time уведомления** через WebSocket
- ✅ **Автоматическое обновление** после операций
- ✅ **Модальные окна** для детальной информации
- ✅ **Автоматическое скрытие** после прочтения

## 🌐 WebSocket интеграция

### Функциональность
- **Real-time уведомления** от сервера
- **Автоматическое переподключение** при разрыве связи
- **JWT аутентификация** для WebSocket соединений
- **Обработка ошибок** и состояний подключения

### События
```javascript
// Получение уведомления
socket.on('notification', (notification) => {
  handleSecureNotification(notification);
});

// Обработка отключения
socket.on('disconnect', (reason) => {
  handleReconnect();
});
```

## 📱 Адаптивность и UX

### Дизайн
- **Современный UI** с градиентами и тенями
- **Адаптивная верстка** для различных устройств (пока в разработке)
- **Консистентная цветовая схема**
- **Интуитивная навигация**

### Интерактивность
- **Анимации загрузки** для всех операций
- **Визуальная обратная связь** для пользователя
- **Предотвращение дублирования** действий
- **Обработка ошибок** с понятными сообщениями

## 🔧 Управление состоянием

### Zustand Store
```javascript
// Основные методы
const {
  isAuth,           // Состояние аутентификации
  user,             // Данные пользователя
  login,            // Вход
  registration,     // Регистрация
  logout,           // Выход
  checkAuth,        // Проверка аутентификации
  fetchUserLogs,    // Получение логов
  updateAvatarsInDOM // Обновление аватаров
} = useAuthStore();
```

### HTTP клиент (Axios)
- **Автоматическое обновление токенов**
- **Обработка ошибок** с редиректами
- **CSRF защита**
- **Interceptors** для токенов

## 🎨 Стилизация

### CSS подход
- **Модульные стили** для компонентов
- **Глобальные стили** в `index.css`
- **Flexbox** для макетов
- **CSS переменные** для цветов и размеров

### Компоненты стилей
- **Градиенты** для кнопок и фонов
- **Тени** для карточек и модальных окон
- **Анимации** для переходов
- **Адаптивные размеры** для различных экранов

## 🚀 Развертывание

### Переменные окружения
```env
VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000
```

### Команды
```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предварительный просмотр сборки
npm run preview
```

## 📋 Основные функции

### Аутентификация
- [x] Вход в систему
- [x] Регистрация
- [x] Реферальные ссылки
- [x] Выход из системы
- [x] Проверка аутентификации

### Управление аккаунтом
- [x] Просмотр профиля
- [x] Редактирование данных
- [x] История входов
- [x] Управление аватарами

### Финансовые операции
- [x] Создание счетов
- [x] Переводы между счетами
- [x] Пополнение счетов
- [x] Вывод средств
- [x] История транзакций

### Отчетность
- [x] Фильтрация отчетов
- [x] Просмотр деталей
- [x] Экспорт данных

### Документооборот
- [x] Загрузка паспортов
- [x] Загрузка документов
- [x] Проверка статуса
- [x] Шифрование данных

### Уведомления
- [x] Real-time уведомления
- [x] Модальные окна
- [x] Автоматическое обновление
- [x] WebSocket интеграция

## 🔒 Безопасность

### Защита данных
- **JWT токены** для аутентификации
- **CSRF токены** для защиты форм
- **Шифрование** чувствительных документов
- **Валидация** на клиенте и сервере

### Безопасность соединений
- **HTTPS/WSS** для продакшена
- **Rate limiting** на WebSocket
- **Проверка токенов** на каждом запросе
- **Автоматическое обновление** токенов

## 🐛 Отладка

### Логирование
- **Консольные логи** для отслеживания состояния
- **Обработка ошибок** с детальной информацией
- **WebSocket события** для мониторинга соединений

### Инструменты разработчика
- **React DevTools** для компонентов
- **Redux DevTools** для Zustand store
- **Network tab** для HTTP запросов
- **Console** для WebSocket событий

## 📈 Производительность

### Оптимизации
- **Lazy loading** компонентов
- **Мемоизация** вычислений
- **Оптимизация рендеринга** React компонентов
- **Кэширование** API запросов

### Мониторинг
- **Метрики загрузки** страниц
- **Время отклика** API
- **Состояние WebSocket** соединений
- **Обработка ошибок** в реальном времени

---

## 👥 Команда разработки

Проект разработан с использованием современных практик React разработки и следует принципам чистого кода, безопасности и производительности.

## 📞 Поддержка

Для получения поддержки или сообщения об ошибках, пожалуйста, обратитесь к команде разработки.